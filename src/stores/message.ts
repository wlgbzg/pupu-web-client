import {reactive} from "vue";
import {defineStore} from "pinia";
import type {Message} from "@/types/beans";
//
// export const useMessageStore = defineStore("message", () => {
//         const message = reactive<Map<string, Message[]>>(new Map());
//         return {message};
//     }
// );
